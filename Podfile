# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Objective-Zip' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Objective-Zip

  pod 'libchardet', :path => "/code/libchardet/"

  target 'Objective-Zip OS X Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Objective-Zip Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

